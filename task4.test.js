const capitalizeString = require('./task4');
// capitalizeString function tests
describe('Is first letter capitalized', () => {
test('a and b adding', () => {
  expect(capitalizeString("burak")).toEqual("Burak");
});
});

